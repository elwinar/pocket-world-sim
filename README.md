# pocket-world-sim

## development setup

need moon.

```
moon run server:build # build the server
bin/server -log info -width 1000 -height 1000 # run the server
```

```
mmon run viewer:serve # serve the viewer
